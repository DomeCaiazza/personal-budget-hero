<tr>
  <td><%= subscription.description %></td>
  <td><%= currency_formatter(subscription.default_amount) %></td>
  <td><%= subscription.subscription_type %></td>
  <td>
    <div class="d-inline-block">
      <%= link_to t('labels.edit'), edit_account_console_subscription_path(subscription.account, subscription), class: "btn btn-sm btn-link d-inline" %>
    </div>
    <div class="d-inline-block">
      <%= button_to t('labels.destroy'), account_console_subscription_path(subscription.account, subscription), method: :delete, class: "btn btn-sm btn-link d-inline" %>
    </div>
  </td>
</tr>