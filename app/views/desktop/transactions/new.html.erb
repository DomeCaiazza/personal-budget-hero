<h2>New <%= @transaction_type.present? ? t("activerecord.labels.transaction.#{@transaction_type}") :
              t("activerecord.models.transaction.one") %></h2>

<%= render 'form', category: @transaction %>

<%= link_to 'Indietro', desktop_transactions_path %>